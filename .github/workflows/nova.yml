# Legacy upstream NOVA CI workflow retained for historical reference only.
# Supernova builds are handled by build-apk.yml and build-preview-apk.yml.
name: Legacy NOVA CI (disabled)

on:
  workflow_dispatch:

jobs:
  legacy-reference:
    if: \${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy upstream NOVA workflow is intentionally disabled for Supernova."
